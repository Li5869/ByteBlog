---
name: "smart-search"
description: "综合搜索工具，自动协调站内文章搜索和外部技术博客搜索。当需要全面获取信息、搜索结果不足或需要跨平台搜索时调用此 Skill。"
---

# 综合搜索 Skill

## 概述

本 Skill 提供**一站式的综合搜索**能力，自动协调站内 Elasticsearch 搜索和外部 Tavily 技术博客搜索。当站内搜索结果不足时，自动补充外部来源，确保用户获得最全面的信息。适用于需要深度调研、信息对比或站内内容不足的场景。

## 触发条件

当用户输入涉及以下意图时，调用本 Skill：

- 需要全面获取某主题的信息（如"帮我全面了解一下 LangGraph"）
- 站内搜索结果较少（如"只搜到 2 篇文章，还想了解更多"）
- 需要外部技术资料补充（如"除了咱们博客，网上还有没有好的资料"）
- 进行技术调研或信息收集（如"调研一下最新的 AI Agent 框架"）
- 跨平台技术对比（如"看看各博客社区对微服务的讨论"）

## 使用到的工具

| 工具函数 | 用途 | 来源文件 |
|---------|------|---------|
| `smart_search_references(keywords, topic="")` | **核心工具**：智能搜索策略，优先站内搜索，结果不足时自动补充外部搜索，最大返回 10 条 | `tools/smart_search_tool.py` |
| `search_articles_by_keyword(keyword, limit=5)` | 单独使用站内 ES 搜索，适合纯站内场景 | `tools/article_tool.py` |
| `search_external_tech_blogs(query)` | 单独使用 Tavily 外部搜索，限定国内技术社区域名 | `tools/article_tool.py` |
| `get_category_list()` | 获取分类列表，辅助限定搜索范围 | `tools/blog_tool.py` |
| `scrape_webpage(url, output_format="markdown", include_metadata=True)` | 爬取单个网页内容，提取正文并转换为 Markdown | `tools/web_scraper_tool.py` |
| `scrape_multiple_webpages(urls, output_format="markdown")` | 批量爬取多个网页内容（最多5个），并发执行 | `tools/web_scraper_tool.py` |

## 工作流程

```
用户搜索请求
    │
    ▼
┌──────────────────────────────────────────────────┐
│              步骤1：关键词提取和扩展               │
│  - 从用户请求中提取核心关键词                       │
│  - 扩展相关关键词（同义词、上下位词）                │
│  - 确定搜索主题（topic）                            │
└──────────────────────┬───────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│              步骤2：执行智能搜索                    │
│                                                   │
│  ┌──────────────────────────────────────────┐    │
│  │  smart_search_references(keywords, topic) │    │
│  │                                           │    │
│  │  ┌─────────┐   ┌──────────────┐         │    │
│  │  │ 站内搜索  │ → │ 结果数量 ≥ 3 │ ← 是 → │    │
│  │  │ (ES)     │   └──────┬──────┘         │    │
│  │  └─────────┘          │ 否               │    │
│  │                       ▼                  │    │
│  │              ┌──────────────┐            │    │
│  │              │ 补充外部搜索  │            │    │
│  │              │ (Tavily)     │            │    │
│  │              └──────────────┘            │    │
│  └──────────────────────────────────────────┘    │
└──────────────────────┬───────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│              步骤3：结果整合                       │
│  - 合并站内和外部搜索结果                          │
│  - 按相关性排序（站内优先）                        │
│  - 标注来源类型（internal/external）              │
│  - 去重处理                                       │
└──────────────────────┬───────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│              步骤4：结果呈现                       │
│  - 按来源分组展示                                  │
│  - 提供简要摘要和链接                              │
│  - 总结核心发现                                    │
└──────────────────────────────────────────────────┘
```

## 搜索策略详解

### 智能搜索协调机制

`smart_search_references` 内部实现了自动协调策略：

1. **站内搜索优先**：
   - 调用 `search_articles_by_keyword(keyword, limit=5)` 搜索 ES
   - 结果标记为 `source: "internal"`
   - 包含：title, summary, author, url, content 等信息

2. **动态决策是否补充外部搜索**：
   - 站内结果 ≥ 3 条 → 直接返回，不下发外部搜索
   - 站内结果 < 3 条 → 自动触发外部搜索补充

3. **外部搜索补充**：
   - 调用 `search_external_tech_blogs(query)` 使用 Tavily 搜索
   - 限定了国内主流技术博客域名（博客园、掘金、CSDN、思否等）
   - 结果标记为 `source: "external"`
   - 最大返回补充后总数不超过 10 条

4. **结果聚合**：
   - 站内结果在前，外部结果在后
   - 按相关性综合排序
   - 自动去重（按标题相似度）

## 使用示例

### 站内结果充足

```
用户：帮我查查博客里关于 Spring Boot 的文章
AI：正在搜索站内文章...
[调用 smart_search_references("Spring Boot", topic="后端")]
[站内找到 5 条结果，充足，无需外部搜索]

📚 **站内搜索结果**（共 5 条）：
1. 📄 《Spring Boot 3.0 新特性详解》 - 作者: 张三
2. 📄 《Spring Boot 整合 MyBatis Plus》 - 作者: 李四
3. 📄 《Spring Boot 项目最佳实践》 - 作者: 王五
...
```

### 站内结果不足，自动补充外部

```
用户：帮我了解一下最新的 AI Agent 框架
AI：正在搜索站内文章...
[调用 smart_search_references("AI Agent 框架", topic="AI")]
[站内仅找到 2 条结果，不足 3 条，自动补充外部搜索...]
[外部搜索补充到 8 条]

📚 **搜索结果汇总**（共 8 条）：

📖 **站内内容**（2 条）：
1. 📄 《LangChain Agent 开发指南》 - 作者: 张三
2. 📄 《构建智能 Agent 工作流》 - 作者: 李四

🌐 **外部资料**（6 条）：
3. 📄 《2026 年 AI Agent 框架对比》 - 来源: 掘金
4. 📄 《AutoGPT 深度解析》 - 来源: CSDN
...
```

## 网页爬取工具使用场景

搜索结果通常只包含摘要信息，当用户需要查看完整内容时，可使用网页爬取工具：

| 场景 | 工具 | 说明 |
|------|------|------|
| 查看外部文章全文 | `scrape_webpage` | 爬取搜索结果中的外部文章链接 |
| 批量获取参考资料 | `scrape_multiple_webpages` | 同时爬取多篇搜索结果的完整内容 |
| 深度阅读特定文章 | `scrape_webpage` | 用户指定某篇文章时获取完整内容 |

**使用示例**：
```
用户：帮我搜一下 LangGraph 的教程，我想看看完整内容
AI：[调用 smart_search_references("LangGraph 教程")]
    [展示搜索结果]
    用户：第3篇文章看起来不错，帮我看看完整内容
    AI：[调用 scrape_webpage(url="https://juejin.cn/xxx")]
        [展示完整文章内容]
```

## 注意事项

- `smart_search_references` 是首选工具，它能自动决定是否需要外部搜索
- 当用户明确要求"只看站内"时，使用 `search_articles_by_keyword` 而非智能搜索
- 当用户明确要求"上网搜"时，可以直接使用 `search_external_tech_blogs`
- 当用户需要查看搜索结果的完整内容时，使用 `scrape_webpage` 爬取
- 外部搜索结果来自国内主流技术社区，经过域名过滤，质量有保障
- Tavily 外部搜索需配置 `tavily_api_key` 环境变量，未配置时自动降级为仅站内搜索
- 综合搜索结果建议先展示站内内容，再展示外部内容，让用户了解信息来源
- 网页爬取工具依赖 `trafilatura` 库，需确保已安装
